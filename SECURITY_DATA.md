# Donnees et confidentialite

Ce depot ne doit pas contenir les microdonnees DIEM, les imports utilisateurs,
les historiques locaux `runs/`, les sorties `outputs/`, les secrets de
pseudonymisation, l'environnement `.venv/` ou les executables.

Le Setup Windows peut etre diffuse ulterieurement via une GitHub Release,
apres validation finale.
